#include <stdio.h>
int main() {
    int arr[] = {1,2,3,4,5,6};
    int sum=0;
 int array_size = sizeof(arr) / sizeof(arr[0]);
    for(int i=0;i<array_size;i++)
    {
    sum=sum+arr[i];}
    printf("%d",sum);}
